#pragma once

// Copy this file to secrets.h in the same folder before flashing locally.
// secrets.h is ignored by git so private Wi-Fi credentials stay off GitHub.

#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
